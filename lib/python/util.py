import config

def noneToString (s):
	# Purpose: if 's' is None, convert it to an empty string; otherwise,
	#	return it as-is
	# Returns: string; see Purpose
	# Assumes: nothing
	# Modifies: nothing
	# Throws: nothing

	if s == None:
		return ''
	return s
